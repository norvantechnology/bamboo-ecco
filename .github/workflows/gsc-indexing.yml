name: 🌿 GSC Auto-Indexing

on:
  push:
    branches:
      - main
  # Allow manual trigger from GitHub UI (Actions tab → Run workflow)
  workflow_dispatch:
    inputs:
      force_all:
        description: "Submit ALL sitemap URLs (ignores diff — useful for first-time setup)"
        required: false
        default: "false"

# Only one indexing run at a time to prevent duplicate submissions
concurrency:
  group: gsc-indexing
  cancel-in-progress: false

# Needed to write the Job Summary
permissions:
  contents: read

jobs:
  index:
    name: Compare Sitemaps & Submit to GSC
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # ──────────────────────────────────────────
      # 1. Checkout
      # ──────────────────────────────────────────
      - name: ⬇️  Checkout repository
        uses: actions/checkout@v4

      # ──────────────────────────────────────────
      # 2. Setup Node (scripts use native ESM — zero npm installs needed)
      # ──────────────────────────────────────────
      - name: 🟢  Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # ──────────────────────────────────────────
      # 3. Restore sitemap snapshot from previous run
      #    Cache miss on first run is fine — the script handles it gracefully
      #    by treating all current URLs as new.
      # ──────────────────────────────────────────
      - name: 💾  Restore previous sitemap snapshot
        id: cache-sitemap
        uses: actions/cache@v4
        with:
          path: scripts/gsc/previous-sitemap-snapshot.xml
          key: sitemap-snapshot-${{ runner.os }}-${{ github.ref_name }}
          restore-keys: |
            sitemap-snapshot-${{ runner.os }}-

      - name: ℹ️  Log snapshot cache status
        run: |
          if [ "${{ steps.cache-sitemap.outputs.cache-hit }}" = "true" ]; then
            echo "✅ Found cached sitemap snapshot from a previous run"
          else
            echo "⚠️  No cached snapshot — all current sitemap URLs will be treated as NEW"
          fi

      # ──────────────────────────────────────────
      # 4. Restore pending-urls.json (overflow queue from previous run)
      # ──────────────────────────────────────────
      - name: 💾  Restore pending-urls queue
        uses: actions/cache@v4
        with:
          path: scripts/gsc/pending-urls.json
          key: pending-urls-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            pending-urls-${{ runner.os }}-${{ github.ref_name }}-

      # ──────────────────────────────────────────
      # 5. Wait for the deployment to be live
      #    The sitemap is generated dynamically from the DB — we need
      #    the LIVE site, not a static file. Poll until it responds HTTP 200.
      # ──────────────────────────────────────────
      - name: ⏳  Wait for deployment to go live
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
        run: |
          echo "Polling $SITE_URL/sitemap.xml ..."
          MAX_TRIES=20
          DELAY=15
          for i in $(seq 1 $MAX_TRIES); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$SITE_URL/sitemap.xml" || echo "000")
            echo "  Attempt $i/$MAX_TRIES — HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              echo "✅ Site is live!"
              break
            fi
            if [ "$i" = "$MAX_TRIES" ]; then
              echo "❌ Site did not come up after $((MAX_TRIES * DELAY))s — aborting"
              exit 1
            fi
            sleep $DELAY
          done

      # ──────────────────────────────────────────
      # 6. Compare sitemaps → produces diff-result.json
      # ──────────────────────────────────────────
      - name: 🔍  Compare old vs new sitemap
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
          FORCE_ALL: ${{ github.event.inputs.force_all }}
        run: |
          if [ "$FORCE_ALL" = "true" ]; then
            echo "⚡ force_all=true — removing old snapshot (all URLs will be submitted)"
            rm -f scripts/gsc/previous-sitemap-snapshot.xml
          fi
          node scripts/gsc/compare-sitemaps.mjs \
            --sitemap-url "$SITE_URL/sitemap.xml" \
            --prev-snapshot scripts/gsc/previous-sitemap-snapshot.xml \
            --output-dir scripts/gsc

      # ──────────────────────────────────────────
      # 7. Submit to Google Indexing API
      #    Results are written to GITHUB_STEP_SUMMARY (visible in Actions UI)
      # ──────────────────────────────────────────
      - name: 📡  Submit URLs to Google Indexing API
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          DAILY_QUOTA: "200"
          REQUEST_DELAY_MS: "1500"
        run: node scripts/gsc/submit-to-gsc.mjs

      # ──────────────────────────────────────────
      # 8. Promote current snapshot → previous (for next run's diff)
      # ──────────────────────────────────────────
      - name: 🔁  Promote sitemap snapshot for next run
        run: |
          if [ -f scripts/gsc/current-sitemap-snapshot.xml ]; then
            cp scripts/gsc/current-sitemap-snapshot.xml scripts/gsc/previous-sitemap-snapshot.xml
            echo "✅ Snapshot promoted: current → previous"
          else
            echo "⚠️  No current snapshot file found to promote"
          fi

      # ──────────────────────────────────────────
      # 9. Save updated snapshot to cache (used as "previous" next push)
      # ──────────────────────────────────────────
      - name: 💾  Save sitemap snapshot for next run
        uses: actions/cache/save@v4
        if: always()
        with:
          path: scripts/gsc/previous-sitemap-snapshot.xml
          key: sitemap-snapshot-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}

      # ──────────────────────────────────────────
      # 10. Save pending-urls.json for next run
      # ──────────────────────────────────────────
      - name: 💾  Save pending-urls queue for next run
        uses: actions/cache/save@v4
        if: always()
        with:
          path: scripts/gsc/pending-urls.json
          key: pending-urls-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}

      # ──────────────────────────────────────────
      # 11. Upload diff result as downloadable artifact (30-day audit trail)
      # ──────────────────────────────────────────
      - name: 📤  Upload diff artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gsc-diff-run-${{ github.run_number }}
          path: |
            scripts/gsc/diff-result.json
            scripts/gsc/current-sitemap-snapshot.xml
          retention-days: 30
